STEPS:

az login --use-device-code
export RESOURCE_GROUP=rg-hack
export COGNITIVE_SERVICE_ACCOUNT=cs-account
export COGNITIVE_SERVICE_ACCOUNT_OPENAI=cs-account-oai

az group create --name rg-hack --location westus
az cognitiveservices account create \
  --name $COGNITIVE_SERVICE_ACCOUNT \
  --resource-group $RESOURCE_GROUP \
  --kind SpeechServices \
  --sku S0 \
  --location westus \
  --yes
az cognitiveservices account keys list \
  --name $COGNITIVE_SERVICE_ACCOUNT \
  --resource-group $RESOURCE_GROUP
az cognitiveservices account show \
  --name $COGNITIVE_SERVICE_ACCOUNT \
  --resource-group $RESOURCE_GROUP \
  --query "properties.endpoint"

sudo apt-get update
sudo apt-get install libssl-dev libasound2
pip install azure-cognitiveservices-speech
pip install scipy
pip install python-dotenv
sudo apt install ffmpeg
pip install openai

ffmpeg -i J.mp3 J.wav
ffmpeg -i nyswonger2.m4a nyswonger2.wav
ffmpeg -i spanish-audio.ogg spanish-audio.wav

export AZURE_SPEECH_KEY=""
export AZURE_SERVICE_REGION="westus"

# Log in to Azure
az login

# Create a resource group (if you don't have one)
az group create --name myResourceGroup --location eastus

# Deploy the model for chatcompletion